{
    "id": "/Customer_tags",
    "type": "object",
    "properties": {
        "customers_id": {"type": "int",  "minLength": 1, "maxLength": 100},
        "tags_id": {"type": "int",  "minLength": 1, "maxLength": 100},
        "notes": {"type": "string",  "minLength": 1, "maxLength": 100}
   

    },
    "required": ["notes"],
    "createColums": ["customers_id", "tags_id", "notes"],
    "updateColums": ["customers_id", "tags_id", "notes"]
}